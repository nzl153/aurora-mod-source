# 贡献指南

感谢你愿意参与完善《奥萝拉》Mod。提交 Issue 或 Pull Request 前，请先读一下这些规则。

## 先读设计文档

**提 bug 前，先看 [docs/DESIGN.md](docs/DESIGN.md) §4「刻意为之，不是 bug」。**
里面列的那些行为都是有意设计的，改掉会连锁出问题。

## 基本原则

- 保持改动小而明确，一次 Pull Request 解决一个问题。
- 不要提交游戏文件、`sts2.dll`、BaseLib、Spine 运行时二进制、PCK 或自动生成目录。
- 不支持 beta 分支；兼容性问题请先在 Steam 正式版复现。
- 提交后由维护者人工审阅，只有明确同意的改动才会进入发布版本。

## 改数值 / 平衡

数值不是凭手感定的，有一套方法（[docs/DESIGN.md](docs/DESIGN.md) §3）：
先统计原版同「稀有度 × 费用」格子的中位数，再看本 mod 偏在哪儿，**只补真正的洼地**。
请在 PR 里说明你的对比依据，不要整体上调。

另外有两条硬约束：

- **稀有度总数不可变**。要动某张卡的稀有度，必须 1 对 1 对调。
- **数值写 `DynamicVar`，不要写 `private const`**。
  否则文案占位符追踪不到，改了代码忘改 `powers.json` 就会出现悬停显示错误。

## 改代码

三条会连锁出事的红线，详见 [docs/DESIGN.md](docs/DESIGN.md) §6–§7：

1. **联机安全**：绝对不要用 `Random` / `DateTime` / `Guid` / `Time.Get*`；
   归属身份一律用 NetId；「每场/每回合一次」必须用持久化标记，否则重连会二次触发。
2. **模块伤害必须保持 Unpowered**。这是防「模块 × 热量 × 剑势」指数爆炸的闸门。
3. **结算顺序**：如果一张卡在攻击结算前修改了热量 / 剑势 / 挑战协议 / 宕机这四样，
   那份变化会影响这次攻击。加「消耗某资源换伤害」的卡时务必手动检查。

构建应当是 **0 错误**。PR 里多出的任何警告都要说明原因。

## 翻译

本地化位于 `Aurora/localization/{eng,zhs,jpn}`。

- 三种语言必须保留完全相同的文件集合与顶层 JSON key。
- 新增 key 时同时补齐英文、简体中文和日文。
- **不要修改卡牌或 Power 的 ID。**
- 用语和标点尽量贴近现有语言文件，也贴近游戏原版的体裁。
- 玩家可见文案里不要出现开发内部术语（债务 / 快照 / 门闩 / 序列化之类）。

## 美术

美术素材采用 CC BY-NC-SA 4.0。提交美术改动请确认是你自己的原创，
并接受以同一协议发布。卡面的画风约定见 [docs/DESIGN.md](docs/DESIGN.md)。

## Bug 与兼容性

请在 Issue 或 Pull Request 中提供：

1. 游戏版本，确认是正式版还是 beta
2. BaseLib 版本
3. 同时启用的其他 mod（模组冲突很常见）
4. 复现步骤，以及是单人还是联机
5. 报错日志（如果有）
